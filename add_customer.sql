-- auto-generated definition
create function add_customer(p_company_id integer, p_first_name character varying, p_last_name character varying, p_email character varying, p_phone character varying) returns integer
    language plpgsql
as
$$
begin
    -- missing source code
end;
$$;

alter function add_customer(integer, varchar, varchar, varchar, varchar) owner to root;

