-- auto-generated definition
create function add_transaction(p_reservation_id integer, p_total numeric, p_method payment_method_type) returns integer
    language plpgsql
as
$$
begin
    -- missing source code
end;
$$;

alter function add_transaction(integer, numeric, payment_method_type) owner to root;

